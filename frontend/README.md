# frontend

Vue 3 + Vite frontend for `wsAI`.

## Start

1. Install dependencies:

```bash
npm install
```

2. Optional: create `.env` from `.env.example` and adjust the backend address.

3. Run dev server:

```bash
npm run dev
```

Default backend base URL is `http://127.0.0.1:9091/api/v1`.
